title   "Merged OPs/s for <%= @disk %> on <%= @_hostname %>"
vtitle   "Merged OPs/s"
area    :first
hide_legend false
<%
  # Create our hostname string in a format that's consistent with what we're storing in Carbon
  # ༼ つ ◕_◕ ༽つ
    ghostname  = (@_hostname + "." + @domain).gsub(/\./, '_')
  # Handle the naming in collectd for HP blade raid arrays
    if @disk =~ /^c0d0/
        writes = "collectd." + ghostname + ".disk-cciss" + @disk + ".disk_merged.write"
        reads = "collectd." + ghostname + ".disk-cciss" + @disk + ".disk_merged.read"
    else 
        writes = "collectd." + ghostname + ".disk-" + @disk + ".disk_merged.write"
        reads = "collectd." + ghostname + ".disk-" + @disk + ".disk_merged.read"
    end
%>
field :Written, :color => "green",
               :alias => "Merged Writes",
               :data  => "scaleToSeconds(nonNegativeDerivative(<%= writes %>),1)"

field :Read, :color => "blue",
               :alias => "Merged Reads",
               :data  => "scaleToSeconds(nonNegativeDerivative(<%= reads %>),1)"
