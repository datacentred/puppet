output {
  if [type] == "syslog" {
  riemann {
      host => "<%= @riemann_dev_host %>"
      riemann_event   => {
        "description" => "%{syslog_message}"
        "service"     => "%{program}"
        "state"       => "%{syslog_severity_code}"
      }
    }
  }
}
