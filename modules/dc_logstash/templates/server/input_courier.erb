input {
 courier {
     port => <%= @port %>
     ssl_verify => true
     ssl_verify_ca => "<%= @cacert %>"
     ssl_certificate => "<%= @cert %>"
     ssl_key => "<%= @key %>"
     type => "log-courier"
 }
}
