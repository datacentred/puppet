filter {
 if [type] == "apache_error" {
  grok {
   patterns_dir => ['<%= scope.lookupvar('dc_logstash::server::logstash_grok_patterns_dir') %>']
   match => [ "message", "%{APACHEERRORLOG}" ]
  }
 }
}
