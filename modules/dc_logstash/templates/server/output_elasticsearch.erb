output {
  elasticsearch {
    host => <%= scope.lookupvar('dc_logstash::server::elasticsearch_host') %>
    embedded => <%= scope.lookupvar('dc_logstash::server::elasticsearch_embedded') %>
    protocol => <%= scope.lookupvar('dc_logstash::server::elasticsearch_protocol') %>
  }
}
