filter {
  if [type] == "apache_error" {
    grok {
      patterns_dir => ['<%= @logstash_grok_patterns_dir %>']
      pattern      => '\[%{HTTPERRORTIMESTAMP:timestamp}\] \[:%{WORD:level}\] \[pid %{NUMBER:pid}\] FastCGI: %{IPORHOST:clientip} %{GREEDYDATA:message}'
    }
  }
}
