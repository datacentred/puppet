output {
  if [type] == "syslog" {
  riemann {
      riemann_event   => {
        "description" => "%{message}"
        "service"     => "%{program}"
        "state"       => "%{syslog_severity_code}"
      }
    }
  }
}
