---
actions:
  1:
    action: allocation
    description: >-
      Apply shard allocation routing to 'require' 'storage_type=hdd' for hot/cold node
      setup for logstash- indices older than <%= @ssd_tier_retention %> days, based on index_creation
      date
    options:
      key: storage_type
      value: hdd
      allocation_type: require
      wait_for_completion: False
      timeout_override:
      continue_if_exception: False
      #disable_action: True
    filters:
    - filtertype: pattern
      kind: prefix
      value: logstash-
      exclude:
    - filtertype: age
      source: creation_date
      direction: older
      unit: days
      unit_count: <%= @ssd_tier_retention %>
      exclude:
