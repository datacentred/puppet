:hierarchy:
    - "modules/%{calling_module}"
    - "releases/%{lsbdistcodename}"
    - "%{environment}"
    - common
:backends:
    - yaml
:yaml:
    :datadir: '/etc/puppet/environments/%{environment}/hieradata'
