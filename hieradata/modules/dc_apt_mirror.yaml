---
dc_apt_mirror::mirrors:
  ubuntu_mirror:
    release:
      - 'trusty'
    components:
      - 'main'
      - 'restricted'
      - 'universe'
      - 'multiverse'
    mirror: "%{hiera('ubuntu_mirror_path')}"
  ubuntu_updates_mirror:
    release:
      - 'trusty-updates'
    components:
      - 'main'
      - 'restricted'
      - 'universe'
      - 'multiverse'
    mirror: "%{hiera('ubuntu_mirror_path')}"
  ubuntu_security_mirror:
    release:
      - 'trusty-security'
    components:
      - 'main'
      - 'restricted'
      - 'universe'
      - 'multiverse'
    mirror: "%{hiera('ubuntu_mirror_path')}"
  ubuntu_backports_mirror:
    release:
      - 'trusty-backports'
    components:
      - 'main'
      - 'restricted'
      - 'universe'
      - 'multiverse'
    mirror: "%{hiera('ubuntu_mirror_path')}"
  ubuntu_debinst_mirror:
    release:
      - 'trusty'
    components:
      - 'main/debian-installer'
      - 'restricted/debian-installer'
      - 'universe/debian-installer'
      - 'multiverse/debian-installer'
    mirror: "%{hiera('ubuntu_mirror_path')}"
  puppetlabs_mirror:
    release:
      - 'trusty'
    components:
      - 'main'
      - 'dependencies'
    mirror: "%{hiera('puppet_mirror_path')}"
  cloudarchive_juno_mirror:
    release:
      - 'trusty-updates/juno'
    components:
      - 'main'
    mirror: "%{hiera('cloudarchive_mirror_path')}"
  virtualbox_mirror:
    release:
      - 'trusty'
    components:
      - 'contrib'
    mirror: "%{hiera('virtualbox_mirror_path')}"
  foreman_mirror:
    release:
      - 'trusty'
    components:
      - 'stable'
    mirror: "%{hiera('foreman_mirror_path')}"
  foreman_trusty_mirror:
    release:
      - 'trusty'
    components:
      - '1.8'
    mirror: "%{hiera('foreman_mirror_path')}"
  foreman_plugins_mirror:
    release:
      - 'plugins'
    components:
      - '1.8'
    mirror: "%{hiera('foreman_mirror_path')}"
  HP_blade_support_mirror:
    release:
      - 'trusty/current'
    components:
      - 'non-free'
    mirror: "%{hiera('hpsupport_mirror_path')}"
  ceph_libapache_mod_fastcgi_mirror:
    release:
      - 'trusty'
    components:
      - 'main'
    mirror: "%{hiera('ceph_libapache_mod_fastcgi_path')}"
  postgresql_mirror:
    release:
      - 'trusty-pgdg'
    components:
      - 'main'
    mirror: "%{hiera('postgres_mirror_path')}"
  mariadb_mirror:
    release:
      - 'trusty'
    components:
      - 'main'
    mirror: "%{hiera('mariadb_mirror_path')}"
  logstash_mirror:
    release:
      - 'stable'
    components:
      - 'main'
    mirror: "%{hiera('apt_source_logstash')}"
  rabbitmq_mirror:
    release:
      - 'testing'
    components:
      - 'main'
    mirror: "%{hiera('rabbitmq_mirror_path')}"
  elasticsearch_mirror:
    release:
      - 'stable'
    components:
      - 'main'
    mirror: "%{hiera('apt_source_elasticsearch')}"
  dell_openmanage_mirror:
    release:
      - 'trusty'
    components:
      - 'openmanage'
    mirror: "%{hiera('dell_openmanage_mirror_path')}"
