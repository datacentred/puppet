---
classes:
  - '::dc_profile::apt::apt'
  - '::dc_profile::util::apparmor'

puppet::version: '3.7.4-1puppetlabs1'

# This value should only be picked up here when we're in a Vagrant VM
neutron::agents::ml2::ovs::local_ip: "%{::ipaddress_eth1}"
