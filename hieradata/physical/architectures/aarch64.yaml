---
excludes:
    - '::collectd::plugin::sensors'
    - '::dc_profile::util::kdump'

nova::compute::vnc_enabled: false
nova::special_service_provider: 'systemd'

neutron::agents::ml2::ovs::local_ip: "%{::ipaddress_bond0_170}" # TODO: This is hardware specific

vswitch::ovs::dkms_ensure: false

dc_nrpe::checks::check_hw::nova_cpu: 8  # Note: platform specific
dc_nrpe::checks::check_hw::nova_mem: 65 # Note: platform specific
