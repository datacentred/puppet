:hierarchy:
    - "nodes/%{::hostname}"
    - "roles/%{::role}"
    - "releases/%{::lsbdistcodename}/%{::architecture}/%{calling_module}"
    - "modules/%{calling_module}"
    - "%{environment}"
    - common
:backends:
    - yaml
:yaml:
    :datadir: '/vagrant/hieradata'
